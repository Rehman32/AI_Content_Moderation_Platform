info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5000/api/v1/auth/register
  body:
    type: json
    data: '{ "firstName": "test", "lastName": "01", "email": "test@gmail.com", "password": "test@123" }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
