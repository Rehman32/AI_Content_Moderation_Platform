info:
  name: Admin Login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5000/api/v1/auth/login
  body:
    type: json
    data: |-
      {
          "email": "admin@gmail.com",
          "password": "admin@123"
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
